import React from 'react';

const BigButtonFill = ({bigButtonText}) => {
   return (
      <div>
         <button class="py-4 px-6 theme-bg text-base rounded-md">{bigButtonText}</button>
      </div>
   );
};

export default BigButtonFill;